# CWB — Code Workbench
# Installation:  pip install -r requirements.txt

# Oberfläche
PySide6>=6.6

# Anbindung an Claude Code
claude-agent-sdk>=0.1

# Sprachausgabe über Edge-TTS (Standardweg, braucht Internet)
edge-tts>=6.1

# Sprachausgabe über SAPI, die Windows-Bordstimme (Rückfall ohne Internet)
pywin32>=306; sys_platform == "win32"

# Töne laufen über winsound, das zur Python-Standardbibliothek unter Windows
# gehört und nicht installiert werden muss.

# Code-Index (index/): lokale semantische Codesuche, kein API-Schlüssel nötig
chromadb>=0.4.24
sentence-transformers>=3.0.0
transformers>=4.41,<5
torch
